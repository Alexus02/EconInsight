{
  "name": "econinsight-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 execute econinsight-db --file=./schema/uploaded-files.sql"
  },
  "devDependencies": {
    "wrangler": "^4.15.2"
  }
}
